{%- comment -%}AUTO-GENERATED BY SPECTRUM STUDIO. DO NOT MODIFY.{%- endcomment -%}

{%- if sai_state != 'draft' and sai_state != 'live' -%}
  <!-- SAI:WRAPPER:STATE_DEFAULT input={{ sai_state | default: 'NIL' }} fell_back_to=live -->
  {%- assign sai_state = 'live' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_vis = 'on' -%}
{%- else -%}
  {%- assign sai_vis = 'off' -%}
{%- endif -%}

{%- if sai_state == 'draft' -%}
  {%- assign sai_handle = 'spectrum_snippet_q2qrnyoo_qg5v5n5ettms550uikpsy6vi_draft' -%}
{%- else -%}
  {%- assign sai_handle = 'spectrum_snippet_q2qrnyoo_qg5v5n5ettms550uikpsy6vi_live' -%}
{%- endif -%}

{%- assign sai_mo = metaobjects.spectrum_snippet_instance[sai_handle] -%}
{%- unless sai_mo -%}
  {%- paginate metaobjects.spectrum_snippet_instance.values by 250 -%}
    {%- for sai_entry in metaobjects.spectrum_snippet_instance.values -%}
      {%- if sai_entry.system.handle == sai_handle -%}
        {%- assign sai_mo = sai_entry -%}
        {%- break -%}
      {%- endif -%}
    {%- endfor -%}
  {%- endpaginate -%}
{%- endunless -%}

{%- if sai_mo and sai_mo.meta.value.status == 'active' -%}
  {%- assign sai_default = nil -%}
  {%- for sai_v in sai_mo.variants.value.variants -%}
    {%- if sai_v.isDefault -%}
      {%- assign sai_default = sai_v -%}
      {%- break -%}
    {%- endif -%}
  {%- endfor -%}

  {%- if sai_default -%}
    {%- capture sai_envelope -%}
    {
    "v":1,
    "instanceId":"qg5v5n5ettms550uikpsy6vi",
    "snippetId":"q2qrnyoo",
    "experienceId":"019dfd41-541b-73ca-bd97-d35ab99f984e"
    ,"variants":{{ sai_mo.variants.value.variants | json }}
    ,"pools":{
    }
    }
    {%- endcapture -%}

    <style data-spectrum-style="qg5v5n5ettms550uikpsy6vi">
    {%- for sai_v in sai_mo.variants.value.variants -%}
      {%- if sai_v.css != blank -%}{{ sai_v.css }}{%- endif -%}
    {%- endfor -%}
    </style>
    <div data-spectrum-instance-id="qg5v5n5ettms550uikpsy6vi" data-spectrum-snippet-id="q2qrnyoo" data-spectrum-variant-id="{{ sai_default.variantId }}" data-spectrum-vis="{{ sai_vis }}" data-spectrum-lq-snippet="mut-qg5v5n5ettms550uikpsy6vi" data-spectrum-experience="_system-164361469987" data-spectrum-feature-slug="banner_carousel_list">
      <script type="application/json" data-spectrum-envelope>{{ sai_envelope | replace: '</script>', '<\/script>' }}</script>
      {% render '_sai-snippet-q2qrnyoo', variant: sai_default, mo: sai_mo, mutation_handle: 'mut-qg5v5n5ettms550uikpsy6vi', experience_handle: '_system-164361469987' %}
    </div>
  {%- endif -%}
{%- endif -%}